---

- name: Add SPF TXT record
  blockinfile:
    path: "/etc/bind/zones/db.{{ domain }}"
    marker: "; {mark} ANSIBLE MANAGED BLOCK: SPF"
    block: "@   IN  TXT  \"v=spf1 a mx ip4:{{ mail_ip }} ~all\""
  when: enable_spf | bool

